# Backend API URL untuk dev mode (npm run dev).
# Prod: nginx forward /api/v1/* langsung ke backend, tidak lewat FE proxy.
VITE_API_URL=http://localhost:7889

# Brevo email OTP (Fase 3 — belum dipakai di Fase 1)
BREVO_API_KEY=
BREVO_SENDER_EMAIL=noreply@tbksanti.id
BREVO_SENDER_NAME="Toko Bahan Kue Santi"
